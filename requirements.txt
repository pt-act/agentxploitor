# AgentxploiTor Requirements
# Core dependencies for the security agent

# Core
python-dotenv>=1.0.0

# Image processing (for visual diff)
pillow>=10.0.0

# Async support (built into Python 3.12+, no external package needed)

# Redis (distributed queue)
redis[hiredis]>=5.0.0

# PostgreSQL (persistent storage)
asyncpg>=0.29.0
alembic>=1.13.0

# HTTP client (for LLM API calls)
aiohttp>=3.9.0

# Observability
prometheus-client>=0.19.0
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp>=1.22.0
opentelemetry-exporter-prometheus>=1.22.0
opentelemetry-instrumentation-aiohttp>=0.43b0
opentelemetry-instrumentation-redis>=0.43b0

# Structured logging
python-json-logger>=2.0.0

# GraphQL
ariadne>=0.21.0

# JWT for GitHub App
PyJWT>=2.8.0
cryptography>=41.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Type checking (optional)
mypy>=1.8.0
types-redis>=4.6.0

# Linting (optional)
ruff>=0.1.0
